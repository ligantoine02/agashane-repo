---
interface Props {
    image: string;
    alt: string;
}

const {image, alt} = Astro.props;
---

<!-- TODO check if image exists -->

{
    image 
    ? <img src={`src/assets/${image}`} alt={alt} class="rounded-xl max-h-60"  /> 
    : <img src="src/assets/placeholder.jpg" alt={alt} class="rounded-xl max-h-60" />
}